import React from "react";

function Grid({src}) {
    return (
        <img 
            src = {src}
            className = "grid"
            alt="description"
        />
    );
}

export default Grid;